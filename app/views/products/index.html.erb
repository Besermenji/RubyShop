<p id="notice"><%= notice %></p>

<h1>Listing Products</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <tr>
    <% @products.each do |product| %>
      <td>
        <!--
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
     <table>
     <thead></thead> 
     <tbody>
      <tr> 
      <td>
         <img vertical_align="middle" src="<%=product.image %>" width="64" height="380">
      </td>
      </tr>
      <tr>
      <td>
      <%=link_to product.name, product %> 
     </td>
     </tr>
     <tr>
     <td>
     <%= form_for( product, :url => "/basket", :html => { :method => :post } ) do |p| %>
       <%= p.hidden_field :id %>
       <%= p.submit %>
     <% end %>
     </td>
     </tr>     
      </tbody>
     </table></td>
    <% end %>
   </tr>
  </tbody>
</table>

<br>
<%if is_admin?%>
<%= link_to 'New Product', new_product_path %>
<%end%>
